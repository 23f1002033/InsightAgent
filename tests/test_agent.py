"""
tests/test_agent.py
───────────────────
Phase 3–5 tests for the LangGraph agent.

Covers:
  • retrieve_documents tool returns chunked docs with metadata
  • web_search tool calls Tavily and returns results
  • StateGraph routes correctly (retrieve → answer, web search → answer)
  • MemorySaver preserves context across turns in the same thread_id
"""
from __future__ import annotations

# Phase 3–5 implementation — placeholder
