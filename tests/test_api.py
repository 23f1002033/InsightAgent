"""
tests/test_api.py
─────────────────
Phase 6 tests for the FastAPI layer.

Covers:
  • GET /health returns 200
  • POST /ingest accepts a file path and returns chunk count
  • POST /chat returns an answer with citations
  • Thread isolation: different thread_ids have independent memory
"""
from __future__ import annotations

# Phase 6 implementation — placeholder
