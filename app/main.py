"""
app/main.py
───────────
FastAPI application for InsightAgent (Phase 6).

Endpoints:
  GET  /health        — liveness probe
  POST /ingest        — load, split, embed, and store documents
  POST /chat          — stateful chat with the LangGraph agent
"""
from __future__ import annotations

# Phase 6 implementation — placeholder
